# Data Layout

The repository does not ship real GWAS or QTL datasets.

Suggested local layout for ad hoc runs:

- `data/gwas/` for GWAS summary statistics
- `data/qtl/` for tabix-indexed QTL files and QTL summary CSVs

For portable work, prefer a standalone project created with `./easycoloc init`.
